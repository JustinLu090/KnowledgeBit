-- ============================================================
-- 固定題目挑戰：儲存發起者的卡片 ID 清單（已洗牌順序）
-- 接受者必須使用相同的卡片清單，確保雙方題目一致、比賽公平
-- ============================================================

ALTER TABLE public.challenge_sessions
  ADD COLUMN IF NOT EXISTS shuffled_card_ids UUID[] DEFAULT '{}';
